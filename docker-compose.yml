services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: labour
      POSTGRES_PASSWORD: labour
      POSTGRES_DB: labour
    ports:
      - "5432:5432"
    volumes:
      - labour_pg:/var/lib/postgresql/data

volumes:
  labour_pg:
